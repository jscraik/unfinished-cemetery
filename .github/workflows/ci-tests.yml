name: CI Tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  jekyll-test-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"
          bundler-cache: true
      - name: Lint headstones schema
        run: ./scripts/lint-headstones.sh --mode ci
      - name: Run jekyll test build
        run: bundle exec jekyll build
