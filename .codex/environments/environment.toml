# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "unfinished-cemetery local environment"

[setup]
script = '''
set -euo pipefail

printf '%s\n' '[codex] setup: /Users/jamiecraik/dev/unfinished-cemetery'

export PATH="/opt/homebrew/bin:/usr/local/bin:$HOME/.local/share/mise/shims:$HOME/.local/bin:$PATH"

if command -v mise >/dev/null 2>&1; then
  if [ -f .mise.toml ]; then
    mise trust --yes .mise.toml >/dev/null 2>&1 || true
  fi
  eval "$(mise activate bash)" >/dev/null 2>&1 || true
fi

missing_tools=0

require_tool() {
  local cmd="$1"
  local hint="$2"
  if ! command -v "$cmd" >/dev/null 2>&1; then
    echo "[codex] missing required tool: ${cmd} (${hint})"
    missing_tools=1
  fi
}

require_tool git "install Git and ensure it is on PATH"
require_tool rg "install ripgrep (rg)"
require_tool jq "install jq"
if ! command -v fd >/dev/null 2>&1 && ! command -v fdfind >/dev/null 2>&1; then
  echo "[codex] missing required tool: fd (or fdfind)"
  missing_tools=1
fi
require_tool ruby "install Ruby runtime"
require_tool bundle "install Bundler for Ruby dependencies"

if [ "$missing_tools" -ne 0 ]; then
  echo "[codex] tooling check failed; install missing tools from tooling.md / mise before continuing."
  exit 1
fi

bundle install || true
'''

[[actions]]
name = "Tooling"
icon = "tool"
command = '''
set -euo pipefail

export PATH="/opt/homebrew/bin:/usr/local/bin:$HOME/.local/share/mise/shims:$HOME/.local/bin:$PATH"

if command -v mise >/dev/null 2>&1; then
  if [ -f .mise.toml ]; then
    mise trust --yes .mise.toml >/dev/null 2>&1 || true
  fi
  eval "$(mise activate bash)" >/dev/null 2>&1 || true
fi

echo "git: $(git --version 2>/dev/null || echo missing)"
echo "rg: $(rg --version 2>/dev/null | head -n 1 || echo missing)"
echo "jq: $(jq --version 2>/dev/null || echo missing)"
if command -v fd >/dev/null 2>&1; then
  echo "fd: $(fd --version 2>/dev/null | head -n 1)"
elif command -v fdfind >/dev/null 2>&1; then
  echo "fd: $(fdfind --version 2>/dev/null | head -n 1) (via fdfind)"
else
  echo "fd: missing"
fi
echo "ruby: $(ruby --version 2>/dev/null || echo missing)"
echo "bundle: $(bundle --version 2>/dev/null || echo missing)"
'''

[[actions]]
name = "Preflight"
icon = "tool"
command = '''
set -euo pipefail

bash scripts/codex-preflight.sh
'''

[[actions]]
name = "Install deps"
icon = "tool"
command = '''
set -euo pipefail

bundle install
'''

[[actions]]
name = "Build site"
icon = "tool"
command = '''
set -euo pipefail

bundle exec jekyll build
'''

[[actions]]
name = "Headstone preflight"
icon = "tool"
command = '''
set -euo pipefail

bash scripts/headstone-preflight.sh
'''

[[actions]]
name = "Run site"
icon = "run"
command = '''
set -euo pipefail

bundle exec jekyll serve
'''
